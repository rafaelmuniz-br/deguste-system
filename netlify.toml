[build]
  base = "app"
  command = "npm run build"
  publish = "dist"
  functions = "netlify/functions"

# SPA: o React Router cuida das rotas
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

[build.environment]
  NODE_VERSION = "22"
